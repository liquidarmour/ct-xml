/*! normalize.css v2.0.1 | MIT License | git.io/normalize */

/* ==========================================================================
   HTML5 display definitions
   ========================================================================== */

/*
 * Corrects `block` display not defined in IE 8/9.
 */

article,
aside,
details,
figcaption,
figure,
footer,
header,
hgroup,
nav,
section,
summary {
    display: block;
}

/*
 * Corrects `inline-block` display not defined in IE 8/9.
 */

audio,
canvas,
video {
    display: inline-block;

}

/*
 * Prevents modern browsers from displaying `audio` without controls.
 * Remove excess height in iOS 5 devices.
 */

audio:not([controls]) {
    display: none;
    height: 0;
}

/*
 * Addresses styling for `hidden` attribute not present in IE 8/9.
 */

[hidden] {
    display: none;
}

/* ==========================================================================
   Base
   ========================================================================== */

/*
 * 1. Sets default font family to sans-serif.
 * 2. Prevents iOS text size adjust after orientation change, without disabling
 *    user zoom.
 * 3. Makes sure colours are being correctly printed on chrome.
 */

html {
    font-family: sans-serif; /* 1 */
    -webkit-text-size-adjust: 100%; /* 2 */
    -ms-text-size-adjust: 100%; /* 2 */
    -webkit-print-color-adjust: exact; /* 3 */
}

/*
 * Removes default margin.
 */

body {
    margin: 0;
}

/* ==========================================================================
   Links
   ========================================================================== */

/*
 * Addresses `outline` inconsistency between Chrome and other browsers.
 */

a:focus {
    outline: thin dotted;
}

/*
 * Improves readability when focused and also mouse hovered in all browsers.
 */

a:active,
a:hover {
    outline: 0;
}

/* ==========================================================================
   Typography
   ========================================================================== */
body {
    font-size: 16px;
    line-height: 1.625em;
}

/*
 * Addresses styling not present in IE 8/9, Safari 5, and Chrome.
 */

abbr[title] {
    border-bottom: 1pt dotted;
}

/*
 * Addresses style set to `bolder` in Firefox 4+, Safari 5, and Chrome.
 */

b,
strong {
    font-weight: bold;
}

/*
 * Addresses styling not present in Safari 5 and Chrome.
 */

dfn {
    font-style: italic;
}

/*
 * Addresses styling not present in IE 8/9.
 */

mark {
    background: #ff0;
    color: #000;
}


/*
 * Corrects font family set oddly in Safari 5 and Chrome.
 */

code,
kbd,
pre,
samp {
    font-family: monospace, serif;
    font-size: 1em;
}

/*
 * Improves readability of pre-formatted text in all browsers.
 */

pre {
    white-space: pre;
    white-space: pre-wrap;
    word-wrap: break-word;
}

/*
 * Sets consistent quote types.
 */

q {
    quotes: "\201C" "\201D" "\2018" "\2019";
}

/*
 * Addresses inconsistent and variable font size in all browsers.
 */

small {
    font-size: 0.811em;
}

/*
 * Prevents `sub` and `sup` affecting `line-height` in all browsers.
 */

sub,
sup {
    font-size: 0.811em;
    position: relative;
    vertical-align: baseline;
}

sup {
    top: -0.5em;
}

sub {
    bottom: -0.25em;
}

a[href],
a[href]:visited {
    color: #005ea5;
}

a[href]:hover {
    color: #2e8aca;
}

/* ==========================================================================
   Embedded content
   ========================================================================== */

/*
 * Removes border when inside `a` element in IE 8/9.
 */

img {
    border: 0;
}

/*
 * Corrects overflow displayed oddly in IE 9.
 */

svg:not(:root) {
    overflow: hidden;
}

/* ==========================================================================
   Figures
   ========================================================================== */

/*
 * Addresses margin not present in IE 8/9 and Safari 5.
 */

figure {
    margin: 0;
}

/* ==========================================================================
   Forms
   ========================================================================== */

/*
 * Define consistent border, margin, and padding.
 */

fieldset {
    border: 1pt solid #c0c0c0;
    margin: 0 0.125em;
    padding: 0.35em 0.625em 0.75em;
}

/*
 * 1. Corrects color not being inherited in IE 8/9.
 * 2. Remove padding so people aren't caught out if they zero out fieldsets.
 */

legend {
    border: 0; /* 1 */
    padding: 0; /* 2 */
}

/*
 * 1. Corrects font family not being inherited in all browsers.
 * 2. Corrects font size not being inherited in all browsers.
 * 3. Addresses margins set differently in Firefox 4+, Safari 5, and Chrome
 */

button,
input,
select,
textarea {
    font-family: inherit; /* 1 */
    font-size: 1em;
    margin: 0; /* 3 */
}

/*
 * Addresses Firefox 4+ setting `line-height` on `input` using `!important` in
 * the UA stylesheet.
 */

button,
input {
    line-height: normal;
}

/*
 * 1. Avoid the WebKit bug in Android 4.0.* where (2) destroys native `audio`
 *    and `video` controls.
 * 2. Corrects inability to style clickable `input` types in iOS.
 * 3. Improves usability and consistency of cursor style between image-type
 *    `input` and others.
 */

button,
html input[type="button"], /* 1 */
input[type="reset"],
input[type="submit"] {
    -webkit-appearance: button; /* 2 */
    cursor: pointer; /* 3 */
}

/*
 * Re-set default cursor for disabled elements.
 */

button[disabled],
input[disabled] {
    cursor: default;
}

/*
 * 1. Addresses box sizing set to `content-box` in IE 8/9.
 * 2. Removes excess padding in IE 8/9.
 */

input[type="checkbox"],
input[type="radio"] {
    box-sizing: border-box; /* 1 */
    padding: 0; /* 2 */
}

/*
 * 1. Addresses `appearance` set to `searchfield` in Safari 5 and Chrome.
 * 2. Addresses `box-sizing` set to `border-box` in Safari 5 and Chrome
 *    (include `-moz` to future-proof).
 */

input[type="search"] {
    -webkit-appearance: textfield; /* 1 */
    -moz-box-sizing: content-box;
    -webkit-box-sizing: content-box; /* 2 */
    box-sizing: content-box;
}

/*
 * Removes inner padding and search cancel button in Safari 5 and Chrome
 * on OS X.
 */

input[type="search"]::-webkit-search-cancel-button,
input[type="search"]::-webkit-search-decoration {
    -webkit-appearance: none;
}

/*
 * Removes inner padding and border in Firefox 4+.
 */

button::-moz-focus-inner,
input::-moz-focus-inner {
    border: 0;
    padding: 0;
}

/*
 * 1. Removes default vertical scrollbar in IE 8/9.
 * 2. Improves readability and alignment in all browsers.
 */

textarea {
    overflow: auto; /* 1 */
    vertical-align: top; /* 2 */
}

/* ==========================================================================
   Tables
   ========================================================================== */

/*
 * Remove most spacing between table cells.
 */

table {
    border-spacing: 0.625em;
}

/*------------------------------------*\
$HELPER
\*------------------------------------*/
/**
* A series of helper classes to use arbitrarily. Only use a helper class if an
* element/component doesn’t already have a class to which you could apply this
* styling, e.g. if you need to float `.main-nav` left then add `float:left;` to
* that ruleset as opposed to adding the `.float--left` class to the markup.
*
* A lot of these classes carry `!important` as you will always want them to win
* out over other selectors.
*/
/**
* Add/remove floats
*/

.float--right {
    float: right!important;
}
.float--left {
    float: left !important;
}
.float--none {
    float: none !important;
}
/**
* Text alignment
*/

.text--left {
    text-align: left !important;
}
.text--center {
    text-align: center!important;
}
.text--right {
    text-align: right !important;
}
/**
* Add/remove margins
*/

.push {
    margin: 0!important;
}
.push--top {
    margin-top: 0!important;
}
.push--right {
    margin-right: 0!important;
}
.push--bottom {
    margin-bottom: 0!important;
}
.push--left {
    margin-left: 0!important;
}
.push--ends {
    margin-top: 0!important;
    margin-bottom: 0!important;
}
.push--sides {
    margin-right: 0!important;
    margin-left: 0!important;
}
.push-half {
    margin: 0!important;
}
.push-half--top {
    margin-top: 0!important;
}
.push-half--right {
    margin-right: 0!important;
}
.push-half--bottom {
    margin-bottom: 0!important;
}
.push-half--left {
    margin-left: 0!important;
}
.push-half--ends {
    margin-top: 0!important;
    margin-bottom: 0!important;
}
.push-half--sides {
    margin-right: 0!important;
    margin-left: 0!important;
}
.flush {
    margin: 0!important;
}
.flush--top {
    margin-top: 0!important;
}
.flush--right {
    margin-right: 0!important;
}
.flush--bottom {
    margin-bottom: 0!important;
}
.flush--left {
    margin-left: 0!important;
}
.flush--ends {
    margin-top: 0!important;
    margin-bottom: 0!important;
}
.flush--sides {
    margin-right: 0!important;
    margin-left: 0!important;
}
/**
* Add/remove paddings
*/

.soft {
    padding: 0!important;
}
.soft--top {
    padding-top: 0!important;
}
.soft--right {
    padding-right: 0!important;
}
.soft--bottom {
    padding-bottom: 0!important;
}
.soft--left {
    padding-left: 0!important;
}
.soft--ends {
    padding-top: 0!important;
    padding-bottom: 0!important;
}
.soft--sides {
    padding-right: 0!important;
    padding-left: 0!important;
}
.soft-half {
    padding: 0!important;
}
.soft-half--top {
    padding-top: 0!important;
}
.soft-half--right {
    padding-right: 0!important;
}
.soft-half--bottom {
    padding-bottom: 0!important;
}
.soft-half--left {
    padding-left: 0!important;
}
.soft-half--ends {
    padding-top: 0!important;
    padding-bottom: 0!important;
}
.soft-half--sides {
    padding-right: 0!important;
    padding-left: 0!important;
}
.hard {
    padding: 0!important;
}
.hard--top {
    padding-top: 0!important;
}
.hard--right {
    padding-right: 0!important;
}
.hard--bottom {
    padding-bottom: 0!important;
}
.hard--left {
    padding-left: 0!important;
}
.hard--ends {
    padding-top: 0!important;
    padding-bottom: 0!important;
}
.hard--sides {
    padding-right: 0!important;
    padding-left: 0!important;
}
.inline-block {
    display: inline-block !important;
}
/**
* Add a help cursor to any element that gives the user extra information on
* `:hover`.
*/

.informative {
    cursor: help!important;
}
/**
* Mute an object by reducing its opacity.
*/

.muted {
    opacity: 0.5!important;
    filter: alpha(opacity 50)!important;
}
/**
* Align items to the right where they imply progression/movement forward, e.g.:
*
*
*/

.proceed {
    text-align: right!important;
}
/**
* Add a right-angled quote to links that imply movement, e.g.:
*
*
*/

.go:after {
    content: "\00A0" "\00BB"!important;
}
/**
* Apply capital case to an element (usually a `strong`).
*/

.caps {
    text-transform: uppercase!important;
}
/**
* Underline text
*/

.underline {
    text-decoration: underline!important;
}

.strong{
    font-weight: bold;
}
.js .js-hidden {
    display: none !important;
}
.no-js .js-visible {
    display: none !important;
}
.center {
    text-align: center;
}

h1,
h2,
h3,
h4,
h5,
h6,
strong,
b {
    font-weight: bold;
}

.content {
    -webkit-font-smoothing: antialiased;
    font-family: 'nta', Arial, sans-serif;
    font-weight: 400;
    margin: 0 auto;
    max-width: 1020px;
    text-transform: none;
}

.content * {
  box-sizing: border-box;
}

.word-break {
    word-wrap: break-word;
}


/*** TYPOGRAPHY ***/
h1,
.heading-large {
    font-size: 2.124em;
    line-height: 1.625em;
    font-weight: 700;
    text-transform: none;

}

h2,
.heading-medium {
    font-size: 1.618em;
    line-height: 1.625em;
    text-transform: none;
    font-weight: 700;
}

h2.heading-small span {
    display: block;
}

h3,
.heading-small {
    font-size: 1.313em;
    line-height: 1.625em;
    text-transform: none;
    font-weight: 700;
}

h3.heading-small {
    background-color: #2b8cc4;
    color: #fff;
    display: inline-block;
    padding: 0.25em 0.5em;
    margin-bottom: 0;
}

p,
h1,
h2,
h3,
h4 {
    margin: 0 0 1.625em 0;
}

pre,
code {
    color: #0b0c0c;
    background-color: #f8f8f8;
    border: 1pt solid #bfc1c3;
    padding: 0.231em 0.538em;
}

.comps-body, .accounts-body {
    font-family: 'Times New Roman';
}

.margin-large {
    margin-top: 0.308em;
}

.margin-xx-large {
    margin-top: 10em;
}

.ct-body {
    font-size: 13px;
}

/*** LAYOUTS ***/

.comps-body, .ct-body, .page-break, .page-break-only {
  page-break-before:always;
}

@@media screen
{
    .comps-body, .accounts-body, .ct-body, .page-break, .page-break-only {
        margin-top: 20em;
    }
}

.no-page-break {
  page-break-inside: avoid;
}

.ct600a-company-info {
      margin-top: 0.5em!important;
}
details {
    clear: both;
}
.page-number {
    display: block;
    text-align: right;
}
.header {
    padding: 0.385em;
}


/*** GRID ***/

  .inner-block {
      margin-left: 1.154em;
      margin-right: 1.154em;
  }
  .grid-wrapper:after {
      content: '';
      display: block;
      clear: both;
  }
  @@media all {
      .grid-wrapper {
          margin-left: -1.154em;
          margin-right: -1.154em;
      }
  }
  .grid-divider {
      border-bottom: 1pt solid #bfc1c3;
      margin-bottom: 1.538em;
      margin-left: 1.154em;
      margin-right: 1.154em;
  }
  .grid-divider:after {
      content: "";
      display: block;
      clear: both;
  }
  @@media all {
      .grid-divider {
          margin-left: 2.308em;
          margin-right: 2.308em;
      }
  }
  .grid {
      float: left;
      width: 100%;
  }
  @@media all {
      .grid-1-4 {
          width: 25%;
      }
  }
  @@media all {
      .grid-1-3 {
          width: 33.33333%;
      }
  }
  @@media all {
      .grid-1-2 {
          width: 50%;
      }
  }
  @@media all {
      .grid-2-3 {
          width: 66.66667%;
      }
  }
  @@media all {
      .grid-3-4 {
          width: 75%;
      }
  }
  .grid .inner-block {
      padding: 0;
  }
  @@media all {
      .grid .inner-block {
          margin: 0 1.154em;
      }
  }
  .inner-block .grid-wrapper {
      padding: 0;
      margin-left: -1.154em;
      margin-right: -1.154em;
  }
  .inner-block .grid-divider {
      margin-left: 0;
      margin-right: 0;
  }

/*** TABLE ***/
table {
    border-collapse: collapse;
    font-size: 1em;
    font-family: Times New Roman;
    width: 100%;
    padding-bottom: 1.154em;
}
th {
    font-weight: bold;
    vertical-align: top;
    text-align: left;
}
td {
    padding: 0.8125em 0;
    text-align: left;
    vertical-align: top;
}

.table-heading {
    text-align: right;
    font-style: italic
}

td.figure,
td.total,
td.total-no-top-border,
td.grandtotal,
td.total-no-top-border,
td.grandtotal-no-top-border,
th.figure {
    vertical-align: middle;
    text-align: right;
    padding: 0.813em 0 0.813em 0.406em;
    white-space: nowrap;
    width: 10%;
}

@@media print {
    td.figure,
    td.total,
    td.total-no-top-border,
    td.grandtotal,
    td.total-no-top-border,
    td.grandtotal-no-top-border,
    th.figure {
        width: 15%;
    }
}

td.description {
    width: 50%;
}
td.total {
    border-top: black solid 1pt;
    border-bottom: black solid 1pt;
}
td.total-no-top-border {
    border-bottom: black solid 1pt;
}
td.grandtotal-no-top-border {
    border-bottom: black double 5pt;
}
td.grandtotal {
    border-top: black solid 1pt;
    border-bottom: black double 5pt;
}
td.align-right {
    text-align: right;
}

.ct600a-table {
    border-top: 1pt solid black;
    padding: 0;
}
.ct600a-table td {
    border-bottom: 1pt solid black;
    border-left: 1pt solid black;
    border-right: 1pt solid black;
    background: white;
    padding: 0 0.231em;
}
.ct600a-table td.clear {
    background: transparent;
    border: 0;
}
.ct600a-table td.last {
    border-left: 0;
}



/*** PANEL ***/
.ct600-panel {
  background-color: #d5e8f3;
  padding: 0.769em;
  margin-bottom: 1em;
}
.ct600-panel:after {
  content: '';
  display: block;
  clear: both;
}


/** FIELD LEVEL ***/
.field-number {
  margin-right: 0.25em;
  font-weight: 700;
}

.calculation-notes {
  background-color: #2b8cc4;
  color: #fff;
  display: block;
  text-align: center;
  margin: 0.385em 0 -0.385em -0.077em;
  font-size: 0.811em;
  padding: 0.4em 0;
}

.calculation-notes.calculation-notes-left {
  margin-left: 0;
}

.form-hint {
  display: inline;
}



.ct600-output {
  display: block;
  background-color: #fff;
  border: 1pt solid #bfc1c3;
  padding: 0.25em 0.5em;
  margin: 0.25em 0 0.35em;
}
.ct600-output:after {
  content: '';
  display: block;
  clear: both;
}
.ct600-output-inline {
    display: inline;
    margin: 0 0.25em 0 0.5em;
}
.ct600-output-large {
  height: 4em;
}
.ct600-output-medium {
  width: 9.8em;
}
.ct600-output-small {
  width: 5em;
}
.ct600-output-small-inline {
  width: 2em;
  display: inline-block;
  margin: 0 0.5em -0.1em 0;
}


.dob {
    margin-right: 0.231em;
    display: inline-block;
    float: left;
    border: 0;
}
.dob .ct600-output-small-inline {
  float: left;
  width: 0.5em;
}
.ct600-output-small-inline.day,
.ct600-output-small-inline.month {
    border-right: 1pt solid #000000;
}

.ct600-output.ct600-output-with-calc-notes {
    padding: 0;
}
.ct600-output-with-calc-notes .calculation-notes {
    margin: 0;
}
.ct600-output-with-calc-notes .box-numbers {
    margin-top: 0;
    margin-bottom: 0;
}



.box-numbers {
    background-color: #2b8cc4;
    color: #fff;
    margin: -0.5em 0.5em -0.5em -0.6em;
    width: 2.5em;
    padding: 0.25em 0;
    text-align: center;
    display: inline-block;
    border: 1pt solid #2b8cc4;
}
.box-numbers-light {
    background-color: #96c6e2;
    color: #005ea5;
}


/** LISTS **/
details,
summary {
    display: block;
}
menu,
ol,
ul {
    margin-top: 0.385em;
    margin-bottom: 1.538em;
    padding: 0;
}
ul,
ol {
    list-style-type: none;
}
ul li {
    margin-bottom: 0.385em;
}
.list-bullet {
    list-style-type: disc;
    margin-left: 1.538em;
}
.list--dashed {
    padding-left: 1.5em;
}
.list--dashed li:before {
    content: '-';
    display: inline-block;
    margin-left: -1.5em;
    width: 1.5em;
}

.logo {
  display: block;
}
 ul.accounts-comps-ct {
   margin-top: 18.462em;
   text-align:center;
}

* {
    position: relative;
    z-index: 2;
}

.draft .ct600-panel {
    z-index: -1;
}

.draft .page-break,
.draft #AccountsHeader,
.draft .accounts-comps-ct {
    zoom: expression(this.runtimeStyle.zoom="1", this.insertBefore(document.createElement("i"), this.firstChild).className="ie-after");
}

.draft .page-break:after,
.draft #AccountsHeader:after,
.draft .accounts-comps-ct:after {
    content: "Draft";
    color: #888;
    display: block;
    font-size: 128px;
    left: 50%;
    line-height: 1em;
    margin: -64px 0 0 -210px;
    opacity: 0.5;
    position: absolute;
    text-transform: uppercase;
    top: 50%;
    -moz-transform: rotate(-30deg);
    -webkit-transform: rotate(-30deg);
    -o-transform: rotate(-30deg);
    -ms-transform: rotate(-30deg);
    transform: rotate(-30deg);
    z-index: 5;
}

.draft .page-break .ie-after,
.draft #AccountsHeader .ie-after,
.draft .accounts-comps-ct .ie-after {
    color: #888;
    display: block;
    filter: alpha(opacity=50);
    font-size: 128px;
    left: 50%;
    line-height: 1em;
    margin: -64px 0 0 -210px;
    position: absolute;
    text-transform: uppercase;
    top: 50%;
    z-index: 5;
    zoom: expression(this.runtimeStyle.zoom="1", this.innerHTML = "DRAFT");
}

.draft #ct600 .page-break:after {
    display: none;
}

.negative,
.debit {
    white-space: nowrap;
}